[package]
name = "markdown-definitions"
version = "0.1.0"
description = "Shared plugin declarations and generated node type metadata."
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
markdown-definitions-derive = { path = "../definitions-derive" }
